@import nib;

global-reset();

@import colors;
@import typography;

html {
  height: 100%;
}

body {
  background-color: c_steel;
  height: 100%;
}

.credits {
  background-color: rgba(255,255,255,.6);
  padding: 10px 20px;
}
